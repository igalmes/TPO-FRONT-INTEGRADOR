const formulario = document.getElementsByClassName('formularioContacto');
const inputs = document.querySelectorAll('#formularioContacto input');

inputs.forEach((input.addEvenListener))
formulario.addEvenListener('Submit',(e) => {
    e.prevent

})